import React from "react";
import { Grid } from "@mui/material";

const Functions = () => {
  return (
    <Grid item xs={8}>
      This is functions page
    </Grid>
  );
};

export default Functions;
